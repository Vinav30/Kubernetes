trigger: none
pool: Vinav

stages:
  - stage: terraformdownload
    
    jobs:
      - job: terraformdownload

        steps:
        - task: TerraformInstaller@1
          inputs:
            terraformVersion: 'latest'